{% extends "base.html.twig" %}
{% block body %}
    <ul>
        {% for player in players %}
            <li><a href=" {{ path('app_player_edit',{'id' : player.id }) }}"> {{ player }} </a></li>

        <br>
    {% endfor %}
    </ul>

    {% for message in app.flashes('success_personne') %}
        {{ message }}
    {% endfor %}
    {% for message in app.flashes('error_personne') %}
        {{ message }}
    {% endfor %}
{% endblock %}